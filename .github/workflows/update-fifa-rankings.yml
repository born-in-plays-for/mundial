name: Update FIFA Rankings (deprecated)

# Replaced by update-elo-rankings.yml — schedule removed to prevent daily failures.
# The FIFA API (api.fifa.com/api/v3) is no longer available (404).
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use update-elo-rankings.yml instead."
